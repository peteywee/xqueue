# Credentials — never commit
.env

# Generated
queue.json
state.json
post.log

# Figures are binary and large; keep them local
media/*
!media/.gitkeep

node_modules/

# npm is not used here — the preinstall guard rejects it,
# but a stray lockfile must never come back.
package-lock.json
yarn.lock
*.swp
.post.lock
*.bak
*.pre-*
*.before-*
*~
env.ts
oauth2.ts
