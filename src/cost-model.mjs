// Estimated X API costs used only for operator-facing projections.
// Keep transport/authentication concerns in xdk-client.mjs.
export const COST = {
  post: 0.015,
  postWithUrl: 0.2,
  ownedRead: 0.001,
};
